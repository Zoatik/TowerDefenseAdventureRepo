using UnityEngine;

public class TopDownCombat : MonoBehaviour
{
    private Enemy target = null;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void UpdateTarget(RaycastHit hit)
    {
        if (target == null)
        {
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
